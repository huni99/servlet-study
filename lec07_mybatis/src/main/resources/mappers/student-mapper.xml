<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gn.mapper.StudentMapper">

	<select id ="selectAll" resultType="com.gn.dto.Student">
		SELECT student_no AS studentNo,
			student_name AS studentName,
			student_age AS studentAge
		FROM student 
	
	</select>
	<select id ="selectOne" parameterType="_int" resultType = "com.gn.dto.Student" >
		SELECT student_no AS studentNo,
			student_name AS studentName,
			student_age AS studentAge
		FROM student 
		WHERE student_no= #{studenNo}
	
	</select>
	<select id="searchList" parameterType="string" resultType= "com.gn.dto.Student">
		SELECT student_no AS studentNo,
			student_name AS studentName,
			student_age AS studentAge
		FROM student 
		WHERE student_name LIKE CONCAT('%',#{name},'%')
		
	</select>
	<!-- 객체가 파라미터로 오면 그냥 그 객체의 필드 명을 써주면 된다  -->
	<insert id="insertStudent" parameterType="com.gn.dto.Student" >
		INSERT INTO student(student_name,student_age)
		VALUES(#{studentName},#{studentAge})
	
	</insert>
	
	<update id="updateStudent" parameterType="com.gn.dto.Student">
		UPDATE student
		SET student_name=  #{studentName},
			student_age =  #{studentAge}
		WHERE student_no = #{studentNo} 
	</update>
	<delete id="deleteStudent" parameterType="_int">
		DELETE 
		FROM student
		WHERE student_no = #{no}
	</delete>
	
	
	
	
	
	
	
	
	
		
</mapper>